Github repo:
https://github.com/tonimoil/harkkatyo

# Toimii ainakin vasta asennetulla ubuntu 20 käyttiksellä.

Mene kansioon, jonne purit .zip paketin
   $ sudo apt-get install libxml2-dev libxslt1-dev
      # Tämä siitä syystä, että requirementsin lxml==4.3.0 ei asennu ilman

   $ sudo apt install python3-flask

   $ sudo apt install python3-pip

   $ pip3 install -r requirements.txt

Tämän jälkeen ohjelman voi suorittaa komennolla
   
   $ FLASK_APP=Turd.py flask run

Mene osoitteeseen <localhost:5000/sign_up>

Tee tunnus käyttäen nimessä ainoastaa aakkosia + anna salasana.
Ohjaudut sivulle login, jonne voit syöttää käyttäjänimen + salasanan.

Jos haluat tehdä useamman tunnuksen, niin mene sign_up sivulle ja toista.
Et kuitenkaan voi olla kirjautuneena sisään, kun yrität mennä ko. sivulle.